# Local dev: Vite proxies /api/trivaller-backend → Spring (see vite.config.js).
# Origin only — no /api. The proxy rewrite already sends paths like /api/auth/login.
VITE_DEV_BACKEND_ORIGIN=http://3.133.90.81:8080

# Optional override. Leave unset to use the proxy path /api/trivaller-backend (recommended).
# VITE_API_BASE_URL=
